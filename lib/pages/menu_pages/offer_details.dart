import 'package:flutter/material.dart';

class OfferDetails extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('offers details')),
        body: Container(
          child: Center(child: Text('details')),
        ));
  }
}
