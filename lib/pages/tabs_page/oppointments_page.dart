import 'package:flutter/material.dart';

class OppointmentsPage extends StatelessWidget {
  OppointmentsPage({this.title});
  final String title;
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text('kkk')),
      ),
    );
  }
}
